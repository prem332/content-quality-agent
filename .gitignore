# Internal planning docs -- not part of the submission, never committed
CLAUDE.md
ARCHITECTURE.md
docs/DESIGN_DECISIONS.md
docs/interview_prep.md
PROJECT_REPORT_FOR_REVIEW.md

# Secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# Runtime-generated (created fresh on each run, not source-controlled)
logs/*.json
logs/*.log
memory/*.json
output/*.md
output/*.json

# Chroma (rebuilt fresh in-memory per run per locked design decision --
# nothing should persist here, but ignore defensively in case of local
# testing with a persist_directory)
chroma_db/
*.sqlite3

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
